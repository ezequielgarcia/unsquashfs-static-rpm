- hosts: localhost
  roles:
  - role: standard-test-basic
    tags:
    - classic
    tests:
    - working:
        dir: .
        run: ansible-playbook --tags=classic wrapper_working.yml
    required_packages:
    - rpmdevtools
    - ansible
